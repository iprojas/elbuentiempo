{
  "name": "elbuentiempo",
  "version": "1.0.0",
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "description": "",
  "scripts": {
    "start": "parcel src/index.html --open",
    "clean": "rm -rf dist/*",
    "build:parcel": "parcel build src/index.html --no-content-hash --no-source-maps --public-url ./",
    "build": "npm run clean && npm run build:parcel"
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/iprojas/[NAME].git"
  },
  "keywords": [],
  "author": "iprojas",
  "license": "MIT",
  "homepage": "https://elbuentiempo.cl",
  "bugs": {
    "url": "https://github.com/iprojas/[NAME]/issues"
  },
  "devDependencies": {
    "parcel": "^2.5.0"
  },
  "dependencies": {
    "gsap": "^3.10.4",
    "imagesloaded": "^5.0.0"
  }
}
